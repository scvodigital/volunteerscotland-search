{{#if @root.data.auth}}
  {{#with @root.data.favourites.items.[0].[0]}}
    {{#> ajax_chip
      onUrl="/toggle-favourites-subscription"
      onMethod="POST"
      offUrl="/toggle-favourites-subscription"
      offMethod="POST"
      onText="Send 'closing soon' alerts"
      offText="Send 'closing soon' alerts"
      iconClasses="far fa-envelope"
      on=this.active
    }}
    {}
    {{/ajax_chip}}
  {{else}}
    You have nothing in your favourites to subscribe to.
   {{/with}}
{{/if}}
