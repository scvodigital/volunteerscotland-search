{
  {{#each @root.context.metaData.terms}}
    "{{key}}": {
    "slugsMap" : {
    {{#each termGroups}}
      {{#each terms}}
        "{{key}}" : {
        "term": "{{{label}}}"
        } {{#unless @last}},{{/unless}}
      {{/each}} {{#unless @last}},{{/unless}}
    {{/each}}
    }
  }
  {{#unless @last}},{{/unless}}
  {{/each}}
}
